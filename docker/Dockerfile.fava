# Fava 后端镜像 - 预安装依赖，无需每次重建
FROM python:3.12-slim

# 预安装系统依赖和 fava
RUN apt-get update && apt-get install -y wget && \
    pip install --no-cache-dir fava && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /data

# 暴露端口
EXPOSE 5001

# 启动命令（通过 docker-compose 覆盖具体的账本文件）
CMD ["fava", "/data/example.beancount", "--host", "0.0.0.0", "--port", "5001"]
